/**
 * Non-Swing field and form model primitives.
 *
 * <p>Text field models keep raw text separate from semantic values so invalid
 * intermediate input remains valid UI state. Form models aggregate typed parse
 * problems and guard result creation.</p>
 */
package cz.auderis.corusco.core.form;
