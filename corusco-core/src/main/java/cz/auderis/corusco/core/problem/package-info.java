/**
 * Typed problem model for parsing, validation, and UI feedback.
 *
 * <p>Problems target forms, fields, rows, cells, and components without using
 * arbitrary field-name strings. The package is Swing-free and leaves rendering
 * and localization to later stages.</p>
 */
package cz.auderis.corusco.core.problem;
