/**
 * Swing examples that are not specific to tables, dialogs, or test helpers.
 *
 * <p>This package covers baseline Swing assembly, field bindings, behaviors,
 * tooltip policy, status and accessible text, busy overlays, and Swing task
 * callbacks. Table, dialog, and testing examples live in dedicated packages to
 * keep each package focused.</p>
 */
package cz.auderis.corusco.examples.swing;
